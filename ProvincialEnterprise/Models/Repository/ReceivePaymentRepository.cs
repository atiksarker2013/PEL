using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace ProvincialEnterprise.Models.Repository
{
    public class ReceivePaymentRepository : Repository<ReceivePayment>
    {
        //public string GetMaxValue(string type)
        //{
        //    return DbSet.Where(x => x.SType == type).Max(x => x == null ? null : x.SID);
        //}
    }
}